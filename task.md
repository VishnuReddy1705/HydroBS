# Phase 8 Task List: Premium Interactive Landing Page (Photo 2 Split-Theme Redesign)

- [x] Install animation dependencies (`gsap`, `lenis`).
- [x] Implement vibrant split-background twilight sky blue gradient (upper section) and clean minimalist white grid layout (lower section) inspired by Photo 2.
- [x] Update canvas ambient flows, spotlight gradients, and particle morphing to match twilight sky tones.
- [x] Configure lower white section with thin borders, dark slate typography, and minimal clean shadow panels.
- [x] Re-architect parametric particle layout mapping to draw a realistic pointed water droplet instead of a heart.
- [x] Unify Axios instances into one single client configuration to prevent asynchronous token refresh conflicts.
- [x] Fix `/api/` pathing suffix bugs inside `TopNavbar.tsx` API requests.
- [x] Implement global dark mode CSS override rules for all dashboard content (`bg-white` card overrides, layout backgrounds, and gray text transitions).
- [x] Verify compile-free builds in production.
